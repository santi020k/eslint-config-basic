---
"@santi020k/eslint-config-basic": minor
"@santi020k/eslint-config-docs": patch
---

Add a `basic-eslint install` command that detects missing framework, feature
category, ESLint, and TypeScript dependencies and installs them with the
project's package manager. Document the command in the v3 setup flow.
