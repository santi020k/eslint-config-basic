/**
 * @deprecated Import these factories from
 * `@santi020k/eslint-config-tools` instead. This compatibility subpath is
 * scheduled for removal in v4.
 */
export {
  command,
  cspell,
  docker,
  githubActions,
  jsdoc,
  nx,
  pnpm,
  prettier,
  swagger
} from '@santi020k/eslint-config-tools'
