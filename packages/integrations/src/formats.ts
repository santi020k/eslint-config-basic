/**
 * @deprecated Import these factories from
 * `@santi020k/eslint-config-formats` instead. This compatibility subpath is
 * scheduled for removal in v4.
 */
export {
  css,
  graphql,
  html,
  jsonc,
  markdown,
  mdx,
  packageJson,
  toml,
  yaml
} from '@santi020k/eslint-config-formats'
