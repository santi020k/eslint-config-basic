/**
 * @deprecated Import these factories from
 * `@santi020k/eslint-config-testing` instead. This compatibility subpath is
 * scheduled for removal in v4.
 */
export {
  cypress,
  jest,
  jestDom,
  playwright,
  testingLibrary,
  vitest
} from '@santi020k/eslint-config-testing'
