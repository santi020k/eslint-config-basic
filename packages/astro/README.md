# @santi020k/eslint-config-astro

Astro specific ESLint rules and configurations.

## Description

This package provides:
- `eslint-plugin-astro` recommended rules.
- Configuration for linting `.astro` files.

## Usage

This package is used automatically when `ConfigOption.Astro` is selected in the main configuration.
