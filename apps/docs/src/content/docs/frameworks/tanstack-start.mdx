---
title: "TanStack Start"
description: "Use TanStack Start support from the lean v3 composer, bundling TanStack Router and TanStack Query rules."
---

import InstallTabs from '../../../components/InstallTabs.astro'

Use TanStack Start support from the lean v3 composer. It bundles TanStack Router and TanStack Query rules for TanStack Start apps built with React or Solid.

## Install


<InstallTabs packages={["@santi020k/eslint-config-basic", "@santi020k/eslint-config-react", "@santi020k/eslint-config-tanstack-start", "eslint"]} />

The command above targets TanStack React Start. For TanStack Solid Start,
replace `@santi020k/eslint-config-react` with
`@santi020k/eslint-config-solid`.


## Configure

```js
import { defineConfig } from '@santi020k/eslint-config-basic'

export default await defineConfig({
  frameworks: {
    react: true,
    'tanstack-start': true
  },
  typescript: true
})
```

With the required config packages installed, the one-line recommended entry auto-detects the framework; custom configs can use `defineConfig()` without a `frameworks` object — pair it with the React or Solid config, and auto-detection enables the right one for you.

## What It Adds

- Recommended rules from `@tanstack/eslint-plugin-router` for route definitions and navigation.
- Recommended rules from `@tanstack/eslint-plugin-query` for query keys, mutations, and cache usage.
- Composition through the same `@santi020k/eslint-config-basic` entry point as every other framework.
- Compatibility with advanced overrides if you pass a custom config array or factory instead of `true`.

## Notes

- Using TanStack Query or Router outside of Start? Enable them individually via the `libraries` option (`'tanstack-query'`, `'tanstack-router'`) instead.
- This framework package is a public, optional dependency in v3; the basic composer loads it only when enabled.

## Related Pages

- [React](/frameworks/react)
- [Solid](/frameworks/solid)
- [Libraries](/tooling/libraries)
