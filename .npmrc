# Hoist ESLint-related packages to root node_modules so ESLint can resolve
# plugins regardless of which workspace package originally declares them.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*@typescript-eslint*

# Don't hard-fail on peer dep version mismatches (some ESLint plugins are loose)
strict-peer-dependencies=false
